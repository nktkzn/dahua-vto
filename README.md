# Dahua DHI-VTO3211D-P2-S2 — Python Examples

English below • Русская версия ниже

## RU
Базовая структура репозитория с примерами Python для домофона **DHI-VTO3211D-P2-S2**.
Откройте папку в VS Code → Source Control → Publish Branch, чтобы выложить на GitHub.

### Быстрый старт
```bash
cd examples/python
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python add_user.py --help  # или другой скрипт
```

---

## EN
Base repository structure with Python examples for **DHI-VTO3211D-P2-S2**.
Open the folder in VS Code → Source Control → Publish Branch to push to GitHub.

### Quick start
```bash
cd examples/python
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python add_user.py --help  # or another script
```
